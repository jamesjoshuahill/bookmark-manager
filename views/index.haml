!!!
%html
  %head
    %title Bookmark Manager
  %body
    %h1 Welcome to Bookmark Manager
    %section
      %h3 Current links:
      %ul
        - @links.each do |link|
          %li
            %a{ href: link.url }= link.title
    #new-link
      %form{ action: '/links', method: 'post' }
        Url:
        %input{ type: 'text', name: 'url' }
        Title:
        %input{ type: 'text', name: 'title' }
        %input{ type: 'submit', value: 'Add link' }